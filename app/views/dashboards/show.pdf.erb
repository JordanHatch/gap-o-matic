<section class="dashboard">
  <%= render partial: 'header.html.erb' %>

  <ul class="entries">
    <% entries.each do |entry| %>
      <%= render partial: 'entry.html.erb', locals: { entry: entry,
                                                      include_links: false } %>
    <% end %>
  </ul>

</section>
